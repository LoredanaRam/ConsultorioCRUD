import { appointmentURL } from "./routes.js";

export default class editFormBuilder {

    // editAppointment = (id) => {
    //     fetch(appointmentURL + "")
    //     document.getElementById('inputEditName').value;
    //     document.getElementById('inputEditReason').value;
    //     document.getElementById('inputEditDescription').value;
    // }
    

    // fetch(appointmentURL, {
    //     method: 'post',
    //     body: JSON.stringify(formData)
    // }).then(()=>{
    //     tableBuilderInstance.rebuildAppointmentTable();
    // })
}